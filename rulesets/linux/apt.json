{
  "tool": "apt",
  "tool_summary": "Debian/Ubuntu's package manager front-end. Installs, upgrades, and removes system packages, typically requiring root, and runs arbitrary maintainer scripts (postinst, preinst, etc.) at install/remove time.",
  "platform": "linux",
  "platform_notes": "apt is Debian/Ubuntu-specific. apt-get is a separate, older binary with largely overlapping behavior and needs its own sibling ruleset per posix_parser.cr's exact-name resolution.",
  "llm_confidence": "high",
  "unknown_flags": [],
  "is_multiplexer": false,
  "option_abbreviations": {},
  "rules": [
    {
      "id": "apt-install-unauthenticated",
      "description": "The --allow-unauthenticated flag (or -y combined with a package from a repository lacking a valid signature) bypasses apt's package signature verification, removing the primary protection against tampered packages.",
      "patterns": [
        {
          "pattern": "apt install --allow-unauthenticated ...",
          "pattern_type": "flag",
          "example": "apt install --allow-unauthenticated somepkg",
          "match": {
            "args_any": [
              "install"
            ],
            "flags_any": [
              "--allow-unauthenticated"
            ]
          }
        }
      ],
      "risk_tags": [
        "elevated-privilege",
        "executes-code",
        "network-egress"
      ],
      "likely_consequences": [
        "unsafe-code-execution",
        "computer-security-compromise"
      ],
      "reversible": "depends",
      "reversible_note": "The package can be removed, but its install-time maintainer scripts may have already made changes beyond the package's own files.",
      "severity": "ERROR",
      "mitre_attack": [
        "T1195.001"
      ]
    },
    {
      "id": "apt-install",
      "description": "Installs a package, running arbitrary maintainer scripts (postinst, etc.) with root privileges at install time.",
      "patterns": [
        {
          "pattern": "apt install ...",
          "pattern_type": "argument",
          "example": "apt install curl",
          "match": {
            "args_any": [
              "install"
            ]
          }
        }
      ],
      "risk_tags": [
        "elevated-privilege",
        "executes-code",
        "network-egress",
        "writes-files"
      ],
      "likely_consequences": [
        "unsafe-code-execution"
      ],
      "reversible": "depends",
      "reversible_note": "The package can be removed, but install-time maintainer scripts may have already made changes beyond the package's own files.",
      "severity": "WARNING",
      "mitre_attack": [
        "T1195.001"
      ]
    },
    {
      "id": "apt-remove-purge",
      "description": "The purge subcommand removes a package along with its configuration files, which is not undone by simply reinstalling.",
      "patterns": [
        {
          "pattern": "apt purge ...",
          "pattern_type": "argument",
          "example": "apt purge somepkg",
          "match": {
            "args_any": [
              "purge"
            ]
          }
        }
      ],
      "risk_tags": [
        "elevated-privilege",
        "deletes-files"
      ],
      "likely_consequences": [
        "data-loss"
      ],
      "reversible": "no",
      "severity": "WARNING",
      "mitre_attack": []
    },
    {
      "id": "apt-package-management",
      "description": "Manages system packages. Read-only subcommands (list, show, search) do not alter system state. Severity here reflects that unrecognized or future apt subcommands fall through to this rule; apt's CLI surface evolves across releases, so an unmatched subcommand should surface for review rather than silently pass as benign.",
      "patterns": [
        {
          "pattern": "apt ...",
          "pattern_type": "argument",
          "example": "apt list --installed",
          "match": {
            "raw_pattern": "^apt(\\s|$)"
          }
        }
      ],
      "risk_tags": [
        "elevated-privilege"
      ],
      "likely_consequences": [],
      "reversible": "yes",
      "severity": "WARNING",
      "mitre_attack": []
    }
  ],
  "default_rule": {
    "risk_tags": [
      "elevated-privilege"
    ],
    "reversible": "yes",
    "severity": "WARNING",
    "mitre_attack": []
  }
}
